import { ProductionTitlePipe } from './production-title.pipe';

describe('ProductionTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductionTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
