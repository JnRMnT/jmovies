import { ToTVSeriesPipe } from './to-tvseries.pipe';

describe('ToTVSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTVSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
